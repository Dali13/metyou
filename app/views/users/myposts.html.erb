<div class="container">
  <div class="row">
		<div class="well">
		  <h1 class="text-center">Posts</h1>
      <% if !@posts.blank? %>
      <%= will_paginate @posts%>
      <div class="list-group">
        <%= render partial: 'posts/post', collection: @posts %>
      </div>
      <%= will_paginate @posts %>
      <% end %>
    </div>
  </div>
</div>